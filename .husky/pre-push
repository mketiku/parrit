# Skipping slow local pre-push checks (Vitest/Build) in favor of CI.
# If you want to run these locally before pushing, use 'npm test' or 'npm run build'.
echo "Validation being performed by GitHub Actions CI..."
npm run lint
